# icones-tests

```bash
pnpm i

pnpm -F lib-icon dev
# => Check out the page, you should see icons

pnpm -F lib-icon build

pnpm -F app dev
# ERROR: Cannot find module 'virtual:icons/mdi/account-tie' imported from '/Users/jycouet/mdev/tmp/icones-tests/lib-icon/dist/LibIcon.js
```
